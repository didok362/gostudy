POST   /books          — добавить книгу
PATCH  /books/{id}     — отметить как прочитанную
GET    /books/{id}     — получить конкретную книгу
GET    /books          — получить все книги (с фильтрацией)
DELETE /books/{id}     — удалить книгу